{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Perastage Debug macOS",
            "type": "lldb",
            "request": "launch",
            "program": "${command:cmake.launchTargetPath}",
            "args": [],
            "cwd": "${workspaceFolder}",
            "stopOnEntry": false
        }
    ]
}